package com.github.dandelion.datatables.core.configuration;

import static org.fest.assertions.Assertions.assertThat;

public class ConfigurationTest {

	// TODO Tester que le ConfigurationPropertiesLoader charge bien les proprietes dans TableConfiguration.
	// TODO Tester que par defaut, le group "global" est bien chargé (une seule fois)
	// TODO Tester que si on définit un autre groupe, il est bien chargé aussi (une seule fois)
	
	public void setup(){
		
	}

}
